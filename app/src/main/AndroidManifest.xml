<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.emperinter.study211">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MixRecyclerViewActivity"
            android:label="@string/title_activity_mix_recycler_view"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".PuRecyclerViewActivity"
            android:label="@string/title_activity_pu_recycler_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GridRecyclerViewActivity" />
        <activity android:name=".HorRecyclerViewActivity" />
        <activity android:name=".LinearRecyclerViewActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".GridViewActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".ImageViewActivity" />
        <activity android:name=".CheckBoxAcitvity" />
        <activity android:name=".RadioButtonActivity" />
        <activity android:name=".EditTextActivity" />
        <activity android:name=".ButtonActivity" />
        <activity android:name=".TextViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>